import React from 'react';
import { CityEntryForm, WeatherCard } from '../../components';

const Weather = () => {

    return(
        <main>
            <CityEntryForm/>
            <WeatherCard/>
        </main>
    )
}

export default Weather;